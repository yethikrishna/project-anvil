{
  "name": "project-anvil",
  "version": "0.1.0",
  "private": true,
  "description": "Federated Alphabet product ecosystem",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "dev:drive": "turbo dev --filter=drive",
    "dev:docs": "turbo dev --filter=docs",
    "dev:youtube": "turbo dev --filter=youtube",
    "dev:maps": "turbo dev --filter=maps",
    "dev:search": "turbo dev --filter=search",
    "dev:gmail": "turbo dev --filter=gmail"
  },
  "devDependencies": {
    "turbo": "^2.4.0",
    "typescript": "^5.7.0"
  },
  "packageManager": "pnpm@9.15.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
